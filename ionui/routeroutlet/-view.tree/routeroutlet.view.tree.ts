namespace $ {
	export class $milkywaystd_ionui_routeroutlet extends $mol_view {
		
		/**
		 * ```tree
		 * dom_name \ion-router-outlet
		 * ```
		 */
		dom_name() {
			return "ion-router-outlet"
		}
		
		/**
		 * ```tree
		 * sub <= getSubs
		 * ```
		 */
		sub() {
			return this.getSubs()
		}
		
		/**
		 * ```tree
		 * getSubs /
		 * ```
		 */
		getSubs() {
			return [
			] as readonly any[]
		}
	}
	
}

