namespace $.$$
{
	/**
	 * Можно стилизовать компоненты и их свойства в TS
	 * @see https://page.hyoo.ru/#!=xwq9q5_f966fg
	 */
	$mol_style_define( $mws_app_spec, {
		FormBlock:{
			display: 'block',
		}
	} )
}
